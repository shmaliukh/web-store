server:
  port: 8082

app:
  webSecurityEnabled: !!bool false
  validationEnabled: !!bool true

logging:
  level:
    root: INFO

spring:
  application:
    name: web-store
  output:
    ansi:
      enabled: always
  profiles:
    active: secrets, database-h2, oath2
